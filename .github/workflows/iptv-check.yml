name: IPTV Checker

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Read playlist
        run: |
          echo "Reading ro.m3u..."
          head -n 10 ro.m3u
